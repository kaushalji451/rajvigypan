/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "rajavigyapan.com", // your logo
      "img.icons8.com",   // icons
      "images.unsplash.com"
    ],
  },
};

export default nextConfig;

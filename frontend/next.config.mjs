/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "rajavigyapan.com", // your logo
      "img.icons8.com",   // icons
    ],
  },
};

export default nextConfig;
